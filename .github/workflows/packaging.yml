name: OpenMortal multi-platform build & release

on:
  push:
    branches: [ upgrade ]
    tags:
      - "v*.*.*"
  pull_request:
    branches: [ upgrade ]
  workflow_dispatch:

env:
  APPIMAGE_EXTRACT_AND_RUN: 1
  VERSION: 0.7.2
  DIST_FOLDER: ../distribution/
jobs:
  linux-${{ env.PLATFORM }}/_build:
    runs-on: ubuntu-latest
    container:
      image: ubuntu:24.04
    env:
      OSNAME: linux-ubuntu-noble
      PLATFORM: ${{ env.PLATFORM }}/

    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          apt update && apt upgrade -y &&
          apt-get install -y wget
          apt install -y build-essential autoconf automake libtool pkg-config dh-autoreconf debhelper devscripts fakeroot build-essential &&
          apt install -y libperl-dev libsdl1.2-dev libsdl-image1.2-dev libsdl-mixer1.2-dev libsdl-net1.2-dev libfreetype6-dev

      - name: Build from source
        run: |
          autoreconf -i -f
          ./configure --prefix=/usr
          make -j$(nproc)

      - name: Create distributable archive
        run: |
          make dist

      - name: make binary files
        run: |
          make DESTDIR=${{ env.DIST_FOLDER }}//linux-${{ env.PLATFORM }}/_build/usr install
          ls -lR ${{ env.DIST_FOLDER }}//linux-${{ env.PLATFORM }}/_build/usr

      - name: Prepare AppImage environment
        run: |
          ls -lR ${{ env.DIST_FOLDER }}//linux-${{ env.PLATFORM }}/_build/usr
          mkdir -p ${{ env.DIST_FOLDER }}//AppDir/usr/bin
          install -m755 ./src/openmortal ${{ env.DIST_FOLDER }}//AppDir/usr/bin/openmortal
          mkdir -p ${{ env.DIST_FOLDER }}//AppDir/usr/share/icons/hicolor/32x32/apps
          install -m644 ${{ env.DIST_FOLDER }}//linux-${{ env.PLATFORM }}/_build/usr/share/openmortal/data/gfx/icon.png ${{ env.DIST_FOLDER }}//AppDir/usr/share/icons/hicolor/32x32/apps/openmortal.png
          mkdir -p ${{ env.DIST_FOLDER }}//AppDir/usr/share/openmortal
          cp -a data ${{ env.DIST_FOLDER }}//AppDir/usr/share/openmortal
          install -m644 packaging/openmortal.AppImage.desktop ${{ env.DIST_FOLDER }}//AppDir/openmortal.desktop
          mkdir -p ${{ env.DIST_FOLDER }}//linux-${{ env.PLATFORM }}/-package/usr/bin
          install -m755 ./src/openmortal ${{ env.DIST_FOLDER }}//linux-${{ env.PLATFORM }}/-package/usr/bin/openmortal
          mkdir -p ${{ env.DIST_FOLDER }}//linux-${{ env.PLATFORM }}/-package/usr/share/openmortal
          cp -a ${{ env.DIST_FOLDER }}//linux-${{ env.PLATFORM }}/_build/usr/openmortal/ ${{ env.DIST_FOLDER }}//linux-${{ env.PLATFORM }}/-package/usr/share/openmortal/
          tar zcvf -C ${{ env.DIST_FOLDER }}//linux-${{ env.PLATFORM }}/-package/ openmortal-{{ env.VERSION }}_linux_${{ env.PLATFORM }}/.tar.gz .

      - name: Download linuxdeploy and appimagetool
        run: |
          wget -qO /tmp/linuxdeploy-x86_64.AppImage "https://github.com/linuxdeploy/linuxdeploy/releases/latest/download/linuxdeploy-x86_64.AppImage"
          chmod +x /tmp/linuxdeploy-x86_64.AppImage
          wget -qO /tmp/appimagetool.AppImage "https://github.com/AppImage/appimagetool/releases/download/continuous/appimagetool-x86_64.AppImage"
          chmod +x /tmp/appimagetool.AppImage

      - name: Create AppImage
        run: |
          /tmp/linuxdeploy-x86_64.AppImage --appdir ${{ env.DIST_FOLDER }}//AppDir --desktop-file=${{ env.DIST_FOLDER }}//AppDir/openmortal.desktop --icon-file=${{ env.DIST_FOLDER }}//AppDir/usr/share/icons/hicolor/32x32/apps/openmortal.png
          /tmp/appimagetool.AppImage ${{ env.DIST_FOLDER }}//AppDir

      - name: Upload AppImage
        uses: actions/upload-artifact@v4
        with:
          name: openmortal_appimage_${{ env.PLATFORM }}/
          path: ../*.AppImage
      #- name: Upload Linux source xz archives for ${{ env.PLATFORM }}/
      #  uses: actions/upload-artifact@v4
      #  with:
      #    name: openmortal_linux_binary_${{ env.PLATFORM }}/.xz
      #    path: ../openmortal-0.7.tar.xz
      - name: Upload Linux source gzip archives for ${{ env.PLATFORM }}/
        uses: actions/upload-artifact@v4
        with:
          name: openmortal_linux_binary_${{ env.PLATFORM }}/.gz
          path: ./openmortal-?.?.tar.gz
      - name: Upload Linux binary gzip archives for ${{ env.PLATFORM }}/
        uses: actions/upload-artifact@v4
        with:
          name: openmortal_linux_binary_${{ env.PLATFORM }}/.gz
          path: openmortal-*_linux_${{ env.PLATFORM }}/.tar.gz
