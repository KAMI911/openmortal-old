name: Flatpak Build (Linux x86_64)

on:
  push:
    branches: [ master, upgrade ]
  pull_request:
    branches: [ master, upgrade ]
  workflow_dispatch:

env:
  VERSION: 0.7.2
  FLATPAK_ID: net.sourceforge.openmortal
  MANIFEST: packaging/net.sourceforge.openmortal.yml

jobs:

  # ── x86_64 Flatpak ──────────────────────────────────────────────────────────
  flatpak-x86_64:
    runs-on: ubuntu-latest
    name: Flatpak x86_64

    steps:
      - uses: actions/checkout@v7

      - name: Install Flatpak and flatpak-builder
        run: |
          sudo apt-get update
          sudo apt-get install -y flatpak flatpak-builder desktop-file-utils

      - name: Add Flathub remote and install x86_64 SDK/runtime
        run: |
          sudo flatpak remote-add --if-not-exists flathub \
            https://flathub.org/repo/flathub.flatpakrepo
          sudo flatpak install -y --arch=x86_64 \
            org.freedesktop.Platform//24.08 \
            org.freedesktop.Sdk//24.08

      - name: Build Flatpak (x86_64)
        run: |
          flatpak-builder \
            --arch=x86_64 \
            --repo=repo \
            --force-clean \
            --ccache \
            --disable-rofiles-fuse \
            builddir-x86_64 \
            "${{ env.MANIFEST }}"

      - name: Export .flatpak bundle (x86_64)
        run: |
          flatpak build-bundle \
            --arch=x86_64 \
            repo \
            "${{ env.FLATPAK_ID }}-${{ env.VERSION }}-x86_64.flatpak" \
            "${{ env.FLATPAK_ID }}"

      - name: Upload Flatpak artifact (x86_64)
        uses: actions/upload-artifact@v7
        with:
          name: ${{ env.FLATPAK_ID }}-${{ env.VERSION }}-x86_64.flatpak
          path: ${{ env.FLATPAK_ID }}-${{ env.VERSION }}-x86_64.flatpak

  # NOTE: there is no i386 Flatpak job. Flathub's org.freedesktop.Platform
  # runtime is only published for x86_64 and aarch64 (confirmed via
  # https://flathub.org/api/v2/summary/org.freedesktop.Platform) — the i386
  # arch was dropped years ago, so an i386 Flatpak build is not possible.
